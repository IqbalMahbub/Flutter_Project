import 'package:flutter/cupertino.dart';

class rating  extends StatelessWidget {
  const rating ({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Rating Fragment"),
      ),
    );
  }
}
